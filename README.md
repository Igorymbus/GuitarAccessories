# MusicStore - Интернет-магазин музыкальных инструментов

Веб-приложение для управления интернет-магазином музыкальных инструментов, разработанное на Django Framework с использованием PostgreSQL в качестве базы данных.

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Настройка базы данных](#настройка-базы-данных)
- [Запуск приложения](#запуск-приложения)
- [Создание администратора](#создание-администратора)
- [Структура проекта](#структура-проекта)
- [API документация](#api-документация)
- [Разработка](#разработка)

## 🔧 Требования

### Системные требования

- **Python** 3.8 или выше
- **PostgreSQL** 12 или выше
- **pip** (менеджер пакетов Python)
- **Git** (опционально, для клонирования репозитория)

### Операционные системы

- Windows 10/11
- Linux (Ubuntu, Debian, CentOS и др.)
- macOS 10.15+

## 📦 Установка

### 1. Клонирование репозитория (если используется Git)

```bash
git clone <repository-url>
cd MusicStore
```

### 2. Создание виртуального окружения

#### Windows:
```bash
python -m venv venv
venv\Scripts\activate
```

#### Linux/macOS:
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
cd MusicStore
pip install -r requirements.txt
```

## 🗄️ Настройка базы данных

### 1. Установка PostgreSQL

Если PostgreSQL не установлен, скачайте и установите с официального сайта:
- Windows: https://www.postgresql.org/download/windows/
- Linux: `sudo apt-get install postgresql postgresql-contrib` (Ubuntu/Debian)
- macOS: `brew install postgresql` (через Homebrew)

### 2. Создание базы данных

Откройте PostgreSQL командную строку (psql) или pgAdmin и выполните:

```sql
CREATE DATABASE DatabaseMSdb;
CREATE USER postgres WITH PASSWORD '1';
ALTER ROLE postgres SET client_encoding TO 'utf8';
ALTER ROLE postgres SET default_transaction_isolation TO 'read committed';
ALTER ROLE postgres SET timezone TO 'UTC';
GRANT ALL PRIVILEGES ON DATABASE DatabaseMSdb TO postgres;
```

### 3. Восстановление базы данных (опционально)

Если у вас есть резервная копия базы данных:

```bash
psql -U postgres -d DatabaseMSdb -f backups/admin_backup.sql
```

### 4. Настройка подключения к базе данных

Откройте файл `MusicStore/MusicStore/settings.py` и при необходимости измените параметры подключения:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'DatabaseMSdb',
        'USER': 'postgres',
        'PASSWORD': '1',  # Измените на свой пароль
        'HOST': 'localhost',
        'PORT': '5432',
        'OPTIONS': {
            'client_encoding': 'UTF8',
        },
    }
}
```

### 5. Применение миграций

```bash
cd MusicStore
python manage.py migrate
```

### 6. Создание представлений и процедур (опционально)

Если у вас есть SQL-скрипты с представлениями и хранимыми процедурами:

```bash
psql -U postgres -d DatabaseMSdb -f docs/database_views.sql
psql -U postgres -d DatabaseMSdb -f docs/database_procedures_triggers.sql
```

## 🚀 Запуск приложения

### Режим разработки

```bash
cd MusicStore
python manage.py runserver
```

Приложение будет доступно по адресу: **http://127.0.0.1:8000/**

### Запуск на другом порту

```bash
python manage.py runserver 8080
```

### Запуск для всех сетевых интерфейсов

```bash
python manage.py runserver 0.0.0.0:8000
```

## 👤 Создание администратора

### Автоматическое создание

Используйте скрипт для создания администратора:

```bash
cd MusicStore
python create_admin.py
```

Следуйте инструкциям в консоли для ввода данных администратора.

### Ручное создание

```bash
python manage.py createsuperuser
```

Введите:
- Username (имя пользователя)
- Email address (адрес электронной почты)
- Password (пароль)

После создания администратора войдите в админ-панель Django:
**http://127.0.0.1:8000/admin/**

## 📁 Структура проекта

```
MusicStore/
├── Apps/                    # Django приложения
│   ├── admin_panel/         # Административная панель
│   ├── cart/                # Корзина покупок
│   ├── catalog/             # Каталог товаров
│   ├── extras/              # Дополнительные функции (отзывы, аналитика)
│   ├── orders/              # Заказы
│   ├── payments/            # Платежи
│   └── users/               # Пользователи и аутентификация
├── main/                    # Основное приложение
├── MusicStore/              # Настройки проекта
│   ├── settings.py          # Конфигурация
│   ├── urls.py             # URL маршруты
│   └── exceptions.py       # Обработка исключений API
├── media/                   # Загружаемые файлы (изображения)
├── backups/                 # Резервные копии БД
├── docs/                    # Документация
├── manage.py               # Управление Django
├── requirements.txt        # Зависимости проекта
└── README.md              # Этот файл
```

## 🔌 API документация

### Базовый URL API

```
http://127.0.0.1:8000/api/
```

### Доступные endpoints

- **Товары:** `/api/products/`
- **Категории:** `/api/categories/`
- **Бренды:** `/api/brands/`
- **Пользователи:** `/api/users/`
- **Корзина:** `/api/carts/`
- **Заказы:** `/api/orders/`
- **Платежи:** `/api/payments/`

### Аутентификация

Для доступа к защищенным endpoints используйте JWT токены:

```bash
# Получение токена
POST http://127.0.0.1:8000/api/auth/token/
{
    "username": "your_username",
    "password": "your_password"
}

# Использование токена в заголовках
Authorization: Bearer <your_token>
```

### Регистрация через API

```bash
POST http://127.0.0.1:8000/api/auth/register/
{
    "username": "new_user",
    "email": "user@example.com",
    "password": "secure_password"
}
```

## 🛠️ Разработка

### Создание миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Создание суперпользователя для разработки

```bash
python manage.py createsuperuser
```

### Запуск тестов

```bash
python manage.py test
```

### Сбор статических файлов (для production)

```bash
python manage.py collectstatic
```

### Просмотр SQL запросов (отладка)

В `settings.py` установите:

```python
DEBUG = True
LOGGING = {
    'version': 1,
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
        },
    },
    'loggers': {
        'django.db.backends': {
            'level': 'DEBUG',
            'handlers': ['console'],
        },
    },
}
```

## 🔐 Безопасность

### Для production окружения

1. **Измените SECRET_KEY** в `settings.py`:
   ```python
   SECRET_KEY = 'your-secret-key-here'
   ```

2. **Установите DEBUG = False**:
   ```python
   DEBUG = False
   ```

3. **Настройте ALLOWED_HOSTS**:
   ```python
   ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']
   ```

4. **Используйте HTTPS** через reverse proxy (nginx, Apache)

5. **Настройте переменные окружения** для чувствительных данных:
   ```python
   import os
   DATABASES['default']['PASSWORD'] = os.getenv('DB_PASSWORD')
   ```

## 📊 Мониторинг

### Prometheus метрики

Метрики доступны по адресу:
```
http://127.0.0.1:8000/metrics
```

## 🐛 Решение проблем

### Ошибка подключения к базе данных

1. Проверьте, что PostgreSQL запущен
2. Убедитесь, что база данных создана
3. Проверьте правильность учетных данных в `settings.py`

### Ошибка импорта модулей

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`

### Ошибки миграций

```bash
# Сброс миграций (осторожно, удалит данные!)
python manage.py migrate --fake-initial
```

### Проблемы с правами доступа

Убедитесь, что пользователь PostgreSQL имеет права на базу данных:
```sql
GRANT ALL PRIVILEGES ON DATABASE DatabaseMSdb TO postgres;
```

## 📝 Дополнительная информация

- **Документация Django:** https://docs.djangoproject.com/
- **Документация DRF:** https://www.django-rest-framework.org/
- **Документация PostgreSQL:** https://www.postgresql.org/docs/

## 👥 Авторы

Разработано в рамках курсового проекта.

## 📄 Лицензия

Проект создан в образовательных целях.

---

**Примечание:** Для production окружения рекомендуется использовать WSGI сервер (например, Gunicorn) с reverse proxy (nginx) и настроить SSL/TLS сертификаты.

